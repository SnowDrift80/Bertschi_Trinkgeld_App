  trinkgeld_app:
    build:
      context: .              # 👈 build from project root
      dockerfile: Dockerfile
    container_name: trinkgeld_app
    restart: unless-stopped
    env_file: .env
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "5012:5012"
